apiVersion: v1
kind: Service
metadata:
  name: {{ include "gotfhub.fullname" . }}
  labels:
    {{- include "gotfhub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gotfhub.selectorLabels" . | nindent 4 }}
